import { NoUserDirective } from './no-user.directive';

describe('NoUserDirective', () => {
  it('should create an instance', () => {
    const directive = new NoUserDirective();
    expect(directive).toBeTruthy();
  });
});
